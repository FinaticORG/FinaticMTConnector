name: Finatic MT Connector CI

on:
  pull_request:
    branches:
      - develop
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: finatic-mt-connector-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint, typecheck, tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Skip heavy CI for Dependabot
        if: github.actor == 'dependabot[bot]'
        run: echo "Skipping heavy product CI for Dependabot (Actions cost control)."
      - name: Checkout repository
        if: github.actor != 'dependabot[bot]'
        uses: actions/checkout@v7

      - name: Install uv
        if: github.actor != 'dependabot[bot]'
        uses: astral-sh/setup-uv@v7
        with:
          version: "latest"

      - name: Setup Python 3.12
        if: github.actor != 'dependabot[bot]'
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Sync dependencies
        if: github.actor != 'dependabot[bot]'
        run: uv sync --all-extras --dev

      - name: Run CI-fast checks
        if: github.actor != 'dependabot[bot]'
        run: uv run poe ci-fast
